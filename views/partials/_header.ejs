<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <% if (username){ %>
          <a class="nav-item nav-link"><form method="POST" action="/logout">
          <a class="nav-item">"You have logged in as: <%= username %>"</a>
          <button type ="submit">Logout</button>
          </form>
          <% } else{ %>
          <a class="nav-item nav-link"><form method="POST" action="/login">
          <input id="id" type="text" name="username" value="" />
          <button type ="submit">Sign In</button>
          </form>
          <% } %>
        </a>
      </div>
    </div>
  </nav>
</header>